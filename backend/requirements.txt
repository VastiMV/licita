# Dependências diretas, versão travada. Sub-dependências ficam a cargo do
# pip a partir destas — ver .venv/ (não versionado) para o que foi
# efetivamente resolvido numa instalação.

Django==6.0.8
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1

# Daphne primeiro em INSTALLED_APPS (settings/base.py) é o que liga o
# `manage.py runserver` a ASGI — necessário para `async def` e Channels.
channels==4.3.2
daphne==4.2.3

# Driver do PostgreSQL (StatefulSet no Kubernetes — ver docs/ARQUITETURA.md).
psycopg[binary]==3.3.4

# Assíncrono — ver docs/ARQUITETURA.md, seção "Assíncrono".
celery==5.6.3
django-celery-beat==2.9.0

# Cliente HTTP dos apps/integracoes (compras.gov.br, PNCP) — mesma lib usada
# e validada no protótipo (branches claude/tenta-de-novo-xu2sxb).
httpx==0.28.1

# Leitura do XLSX de notas CAPAG do Tesouro Nacional (apps/capag/sync.py —
# não tem API, só arquivo estático, ver docs/DOMINIO.md) e escrita da
# planilha de proposta do Cotador (apps/cotador/planilha.py).
openpyxl==3.1.5

# Exigida pelo openpyxl para embutir imagem numa planilha — é o logo no
# cabeçalho da proposta exportada pelo Cotador. Sem ela o openpyxl levanta
# ImportError na hora de inserir a imagem (não é opcional nesse caminho).
Pillow==11.3.0

# Cliente S3 do driver de bucket (apps/armazenamento/drivers/s3.py) — serve
# Cloudflare R2, AWS S3, MinIO e Spaces, que falam o mesmo protocolo. É
# importado dentro do método, não no topo: sem ele o produto sobe e a tela
# mostra o driver como indisponível, em vez de quebrar na hora do upload.
boto3==1.43.98

# Cifra dos segredos da configuração de armazenamento
# (apps/armazenamento/cripto.py). Já vinha como sub-dependência do
# simplejwt; está aqui porque agora é uso direto.
cryptography==50.0.1
