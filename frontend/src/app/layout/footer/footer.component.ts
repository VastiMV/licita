import { Component } from '@angular/core';

/** Rodapé institucional — mesmo em toda página, autenticada ou não. */
@Component({
  selector: 'app-footer',
  templateUrl: './footer.component.html',
  styleUrl: './footer.component.scss',
})
export class FooterComponent {}
